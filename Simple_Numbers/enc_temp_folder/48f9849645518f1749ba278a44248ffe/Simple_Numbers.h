#pragma once
#include <chrono>
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
using CPPFanAlgorithm = vector <int>(*)(vector <int>);
vector <int> FindPrimitiveNumbers(vector <int>);